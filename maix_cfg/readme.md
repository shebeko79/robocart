chmod a-x /etc/init.d/S30wifi
chmod a-x /etc/init.d/S06maixapp

chmod a+x /etc/init.d/S30wifidyn
chmod a+x /etc/init.d/S81robot

chmod a+x /bin/wifi_action.sh

echo "robocart" >/boot/hostname
